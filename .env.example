# StoryToon environment (copy to .env.local)

# Optional — enables production Google Sign-In (demo mode works without these)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional — live Gemini generation (mock comics used when empty)
GEMINI_API_KEY=

# Per authenticated user daily generation cap
DAILY_GENERATION_LIMIT=5
